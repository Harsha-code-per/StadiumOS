# Gemini API Key (Required for reasoning layer fallback cascade)
# You can provide a single key or a comma-separated list of keys for rotation
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Server host and port configurations
HOST=127.0.0.1
PORT=8000
